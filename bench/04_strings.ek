import "./bench_utils.ek" as bench;

let start = bench.nowMs();

let builder = str.builder();
let i = 0;
while (i < 20000) {
  str.append(builder, "abc");
  i = i + 1;
}

let text = str.build(builder);
bench.report("string", start);
