fun add(a, b) {
  return a + b;
}

let task = spawn(add, 2, 3);
print("await", await(task));

let ch = channel();
send(ch, "a");
send(ch, "b");
print("recv", recv(ch), recv(ch), recv(ch));

sleep(0);
print("sleep", "ok");
