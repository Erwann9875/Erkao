print("hello from erkao");

let name = "Erkao";
print("Welcome, " + name + "!");

let xs = [1, 2, 3];
xs[3] = 4;
print("len", len(xs));

let data = { project: "Erkao", version: "0.1" };
print(data["project"]);
